import { Queue } from '../data_structures/Queue'

// binary search we continue to dive the search interval by half

// linear search sequentially evaluates each item in a list for a target value until it has been matched or all items have been searched
// time complexity: 0(n)